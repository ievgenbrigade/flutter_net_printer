name: flutter_net_printer
description: The library allow you to discover and print to network printers using Flutter.
version: 1.0.4
homepage: https://github.com/fuadreza/flutter_net_printer

environment:
  sdk: ^3.7.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  network_info_plus: ^7.0.0
  plugin_platform_interface: ^2.1.8

dev_dependencies:
  build_runner: ^2.4.15
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  mockito: ^5.4.6

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' specifies the class (in Java, Kotlin, Swift, Objective-C, etc.)
  # which should be registered in the plugin registry. This is required for
  # using method channels.
  # The Android 'package' specifies package in which the registered class is.
  # This is required for using method channels on Android.
  # The 'ffiPlugin' specifies that native code should be built and bundled.
  # This is required for using `dart:ffi`.
  # All these are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: top.fuadreza.flutternetprinter.flutter_net_printer
        pluginClass: FlutterNetPrinterPlugin
      ios:
        pluginClass: FlutterNetPrinterPlugin
      linux:
        pluginClass: FlutterNetPrinterPlugin
      macos:
        pluginClass: FlutterNetPrinterPlugin
      windows:
        pluginClass: FlutterNetPrinterPluginCApi
